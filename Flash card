<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title></title>
</head>

<body>
  
</body>

</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flashcard Quiz App</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        .app-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 30px;
            max-width: 800px;
            width: 100%;
            min-height: 500px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #333;
            margin-bottom: 10px;
            font-size: 2.5em;
        }

        .header p {
            color: #666;
            font-size: 1.1em;
        }

        .flashcard-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 30px;
        }

        .flashcard {
            background: #f8f9fa;
            border: 2px solid #e9ecef;
            border-radius: 15px;
            width: 100%;
            max-width: 500px;
            min-height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 30px;
            margin-bottom: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
        }

        .flashcard:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }

        .flashcard.flipped {
            background: #e8f5e8;
            border-color: #28a745;
        }

        .flashcard-content {
            font-size: 1.3em;
            line-height: 1.6;
            color: #333;
        }

        .card-indicator {
            position: absolute;
            top: 15px;
            right: 20px;
            background: #667eea;
            color: white;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: bold;
        }

        .controls {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .btn {
            background: #667eea;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 25px;
            cursor: pointer;
            font-size: 1em;
            font-weight: bold;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn:hover {
            background: #5a67d8;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .btn:disabled {
            background: #ccc;
            cursor: not-allowed;
            transform: none;
        }

        .btn.secondary {
            background: #6c757d;
        }

        .btn.secondary:hover {
            background: #5a6268;
        }

        .btn.danger {
            background: #dc3545;
        }

        .btn.danger:hover {
            background: #c82333;
        }

        .btn.success {
            background: #28a745;
        }

        .btn.success:hover {
            background: #218838;
        }

        .card-form {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 20px;
            border: 2px solid #e9ecef;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #333;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1em;
            font-family: inherit;
        }

        .form-group textarea {
            min-height: 80px;
            resize: vertical;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .card-list {
            margin-top: 30px;
        }

        .card-item {
            background: white;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s ease;
        }

        .card-item:hover {
            border-color: #667eea;
            transform: translateY(-2px);
        }

        .card-content {
            flex: 1;
        }

        .card-question {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .card-answer {
            color: #666;
            font-style: italic;
        }

        .card-actions {
            display: flex;
            gap: 10px;
        }

        .empty-state {
            text-align: center;
            padding: 50px 20px;
            color: #666;
        }

        .empty-state h3 {
            margin-bottom: 10px;
            font-size: 1.5em;
        }

        .progress-bar {
            background: #e9ecef;
            height: 8px;
            border-radius: 4px;
            margin: 20px 0;
            overflow: hidden;
        }

        .progress-fill {
            background: #667eea;
            height: 100%;
            border-radius: 4px;
            transition: width 0.3s ease;
        }

        @media (max-width: 600px) {
            .app-container {
                padding: 20px;
                margin: 10px;
            }

            .header h1 {
                font-size: 2em;
            }

            .controls {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }

            .card-item {
                flex-direction: column;
                gap: 15px;
                align-items: flex-start;
            }

            .card-actions {
                width: 100%;
                justify-content: flex-end;
            }
        }
    </style>
</head>
<body>
    <div class="app-container">
        <div class="header">
            <h1>📚 Flashcard Quiz App</h1>
            <p>Study smarter with interactive flashcards</p>
        </div>

        <div id="quiz-view" class="quiz-view">
            <div class="flashcard-container">
                <div class="flashcard" id="flashcard" onclick="toggleCard()">
                    <div class="card-indicator" id="card-indicator">Card 1 of 0</div>
                    <div class="flashcard-content" id="flashcard-content">
                        <div class="empty-state">
                            <h3>No flashcards yet!</h3>
                            <p>Create your first flashcard to get started.</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="progress-bar" id="progress-container" style="display: none;">
                <div class="progress-fill" id="progress-fill"></div>
            </div>

            <div class="controls">
                <button class="btn secondary" id="prev-btn" onclick="previousCard()" disabled>← Previous</button>
                <button class="btn success" id="show-answer-btn" onclick="toggleCard()" disabled>Show Answer</button>
                <button class="btn secondary" id="next-btn" onclick="nextCard()" disabled>Next →</button>
            </div>

            <div class="controls">
                <button class="btn" onclick="showAddForm()">+ Add Card</button>
                <button class="btn secondary" onclick="showManageCards()">Manage Cards</button>
            </div>
        </div>

        <div id="add-form" class="add-form" style="display: none;">
            <h2 style="margin-bottom: 20px; color: #333;">Add New Flashcard</h2>
            
            <div class="card-form">
                <div class="form-group">
                    <label for="question-input">Question:</label>
                    <textarea id="question-input" placeholder="Enter your question here..." required></textarea>
                </div>
                
                <div class="form-group">
                    <label for="answer-input">Answer:</label>
                    <textarea id="answer-input" placeholder="Enter the answer here..." required></textarea>
                </div>
                
                <div class="controls">
                    <button class="btn success" onclick="saveCard()">Save Card</button>
                    <button class="btn secondary" onclick="cancelAdd()">Cancel</button>
                </div>
            </div>
        </div>

        <div id="manage-view" class="manage-view" style="display: none;">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
                <h2 style="color: #333;">Manage Flashcards</h2>
                <button class="btn secondary" onclick="backToQuiz()">← Back to Quiz</button>
            </div>
            
            <div id="cards-list" class="card-list"></div>
        </div>

        <div id="edit-form" class="edit-form" style="display: none;">
            <h2 style="margin-bottom: 20px; color: #333;">Edit Flashcard</h2>
            
            <div class="card-form">
                <div class="form-group">
                    <label for="edit-question-input">Question:</label>
                    <textarea id="edit-question-input" required></textarea>
                </div>
                
                <div class="form-group">
                    <label for="edit-answer-input">Answer:</label>
                    <textarea id="edit-answer-input" required></textarea>
                </div>
                
                <div class="controls">
                    <button class="btn success" onclick="updateCard()">Update Card</button>
                    <button class="btn secondary" onclick="cancelEdit()">Cancel</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        let flashcards = [];
        let currentCardIndex = 0;
        let showingAnswer = false;
        let editingIndex = -1;

        // Sample data to get started
        flashcards = [
            {
                question: "What is the capital of France?",
                answer: "Paris"
            },
            {
                question: "What is 2 + 2?",
                answer: "4"
            },
            {
                question: "What programming language is used for web styling?",
                answer: "CSS (Cascading Style Sheets)"
            }
        ];

        function updateDisplay() {
            const flashcard = document.getElementById('flashcard');
            const content = document.getElementById('flashcard-content');
            const indicator = document.getElementById('card-indicator');
            const showAnswerBtn = document.getElementById('show-answer-btn');
            const prevBtn = document.getElementById('prev-btn');
            const nextBtn = document.getElementById('next-btn');
            const progressContainer = document.getElementById('progress-container');
            const progressFill = document.getElementById('progress-fill');

            if (flashcards.length === 0) {
                content.innerHTML = `
                    <div class="empty-state">
                        <h3>No flashcards yet!</h3>
                        <p>Create your first flashcard to get started.</p>
                    </div>
                `;
                indicator.textContent = 'No cards';
                showAnswerBtn.disabled = true;
                prevBtn.disabled = true;
                nextBtn.disabled = true;
                progressContainer.style.display = 'none';
                flashcard.classList.remove('flipped');
                return;
            }

            const currentCard = flashcards[currentCardIndex];
            indicator.textContent = `Card ${currentCardIndex + 1} of ${flashcards.length}`;
            
            if (showingAnswer) {
                content.textContent = currentCard.answer;
                showAnswerBtn.textContent = 'Show Question';
                flashcard.classList.add('flipped');
            } else {
                content.textContent = currentCard.question;
                showAnswerBtn.textContent = 'Show Answer';
                flashcard.classList.remove('flipped');
            }

            showAnswerBtn.disabled = false;
            prevBtn.disabled = currentCardIndex === 0;
            nextBtn.disabled = currentCardIndex === flashcards.length - 1;

            // Update progress bar
            progressContainer.style.display = 'block';
            const progress = ((currentCardIndex + 1) / flashcards.length) * 100;
            progressFill.style.width = progress + '%';
        }

        function toggleCard() {
            if (flashcards.length === 0) return;
            showingAnswer = !showingAnswer;
            updateDisplay();
        }

        function nextCard() {
            if (currentCardIndex < flashcards.length - 1) {
                currentCardIndex++;
                showingAnswer = false;
                updateDisplay();
            }
        }

        function previousCard() {
            if (currentCardIndex > 0) {
                currentCardIndex--;
                showingAnswer = false;
                updateDisplay();
            }
        }

        function showAddForm() {
            document.getElementById('quiz-view').style.display = 'none';
            document.getElementById('add-form').style.display = 'block';
            document.getElementById('manage-view').style.display = 'none';
            document.getElementById('edit-form').style.display = 'none';
            document.getElementById('question-input').focus();
        }

        function saveCard() {
            const question = document.getElementById('question-input').value.trim();
            const answer = document.getElementById('answer-input').value.trim();

            if (!question || !answer) {
                alert('Please fill in both question and answer fields.');
                return;
            }

            flashcards.push({ question, answer });
            
            // Clear form
            document.getElementById('question-input').value = '';
            document.getElementById('answer-input').value = '';
            
            // Go back to quiz view
            backToQuiz();
            
            // If this was the first card, set it as current
            if (flashcards.length === 1) {
                currentCardIndex = 0;
            }
            
            updateDisplay();
        }

        function cancelAdd() {
            document.getElementById('question-input').value = '';
            document.getElementById('answer-input').value = '';
            backToQuiz();
        }

        function showManageCards() {
            document.getElementById('quiz-view').style.display = 'none';
            document.getElementById('add-form').style.display = 'none';
            document.getElementById('manage-view').style.display = 'block';
            document.getElementById('edit-form').style.display = 'none';
            updateCardsList();
        }

        function updateCardsList() {
            const cardsList = document.getElementById('cards-list');
            
            if (flashcards.length === 0) {
                cardsList.innerHTML = `
                    <div class="empty-state">
                        <h3>No flashcards to manage</h3>
                        <p>Add some flashcards first to manage them here.</p>
                    </div>
                `;
                return;
            }

            cardsList.innerHTML = flashcards.map((card, index) => `
                <div class="card-item">
                    <div class="card-content">
                        <div class="card-question">${card.question}</div>
                        <div class="card-answer">${card.answer}</div>
                    </div>
                    <div class="card-actions">
                        <button class="btn" onclick="editCard(${index})">Edit</button>
                        <button class="btn danger" onclick="deleteCard(${index})">Delete</button>
                    </div>
                </div>
            `).join('');
        }

        function editCard(index) {
            editingIndex = index;
            const card = flashcards[index];
            
            document.getElementById('edit-question-input').value = card.question;
            document.getElementById('edit-answer-input').value = card.answer;
            
            document.getElementById('quiz-view').style.display = 'none';
            document.getElementById('add-form').style.display = 'none';
            document.getElementById('manage-view').style.display = 'none';
            document.getElementById('edit-form').style.display = 'block';
        }

        function updateCard() {
            const question = document.getElementById('edit-question-input').value.trim();
            const answer = document.getElementById('edit-answer-input').value.trim();

            if (!question || !answer) {
                alert('Please fill in both question and answer fields.');
                return;
            }

            flashcards[editingIndex] = { question, answer };
            editingIndex = -1;
            
            showManageCards();
            updateDisplay();
        }

        function cancelEdit() {
            editingIndex = -1;
            showManageCards();
        }

        function deleteCard(index) {
            if (confirm('Are you sure you want to delete this flashcard?')) {
                flashcards.splice(index, 1);
                
                // Adjust current card index if necessary
                if (currentCardIndex >= flashcards.length) {
                    currentCardIndex = Math.max(0, flashcards.length - 1);
                }
                
                showingAnswer = false;
                updateCardsList();
                updateDisplay();
            }
        }

        function backToQuiz() {
            document.getElementById('quiz-view').style.display = 'block';
            document.getElementById('add-form').style.display = 'none';
            document.getElementById('manage-view').style.display = 'none';
            document.getElementById('edit-form').style.display = 'none';
        }

        // Keyboard navigation
        document.addEventListener('keydown', function(event) {
            if (document.getElementById('quiz-view').style.display !== 'none') {
                switch(event.key) {
                    case 'ArrowLeft':
                        previousCard();
                        break;
                    case 'ArrowRight':
                        nextCard();
                        break;
                    case ' ':
